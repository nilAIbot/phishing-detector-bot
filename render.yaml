services:
  - type: web
    name: phishsleuth-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.inference.server:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.7